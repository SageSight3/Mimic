use crate::modules::map;
use rand::Rng;

#[derive(Debug)]
pub struct MapGenerator {
    map: map::Map,
    //check issue list
    generation_specification: String,
    //check issue list
    eon: String,
    pass: i32
}

impl MapGenerator {
    pub fn new() {

    }

    pub fn generate_map() {

    }
}
